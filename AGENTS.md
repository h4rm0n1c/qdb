# Agent Notes

- Keep project documentation in `docs/` up to date as changes are made.
- Prefer adding new operational knowledge (setup, schema, workflows) to `docs/` instead of inline comments.
- Do not add binary files to the repository.
